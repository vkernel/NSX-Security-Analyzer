# Legacy CLI-imported environment credentials, used by the audit worker only.
# New environments use username/password fields in the application.
# Use single quotes around values with $ or # when writing Compose env files.
NSX_USERNAME_EAST='admin'
NSX_PASSWORD_EAST=''
NSX_USERNAME_WEST='admin'
NSX_PASSWORD_WEST=''
